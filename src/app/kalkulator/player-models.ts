export interface PlayerStat {
    name: string;
    value: number;
}

export interface PlayerKlasa {
    name: string;
    color: string;
}

export interface PlayerDamageType {
    name: string;
    color: string;
}